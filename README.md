# Daniel-makerx Tap

## How do I install these formulae?

`brew install daniel-makerx/tap/<formula>`

Or `brew tap daniel-makerx/tap` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
